import {REQUEST_METHODS} from '../../util/config';

export const ROUTES_PREFIX = '/roles';
export const GET_ALL = {
	route: '/getAll',
	request_method: REQUEST_METHODS.GET
};

export const GET = {
	route: '/getCurrentRole',
	request_method: REQUEST_METHODS.GET
};
